# Routing lab with FRR

## Notes

- `/etc/init.d/frr` needs python installed
- `vtysh` will not load a hostname from the config if it doesn't match the OS `hostname`

## ISIS

- `ip router isis {area-tag}`
